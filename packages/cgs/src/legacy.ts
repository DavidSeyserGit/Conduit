/**
 * Conduit 0.3 contracts retained only for lossless development-data migration
 * and feature-parity execution during the 0.4 release candidate.
 *
 * New clients must import the canonical CGS types from `@conduit/cgs`.
 */
export * from "./legacy-contracts.js";
